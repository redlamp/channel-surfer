# 2026-08-24

## Session moved home

- This repo is now the session root (chat history for the build-out
  stays filed under the old ImageViewerGigi project; see
  [[../notes/session-handoff-2026-08-24|session-handoff]]).

## Hexagon → Inspector → widget (the day's main thread)

- Hexagon hover-card refinements: parks outside the hovered tile
  (edge columns go outward into letterbox space), locked per tile,
  stem tips filled with the wheel color under them, pinned color draws
  a dashed full-alpha construction chain for comparison.
- Hue line: white dashed stem + white handle around a final-color
  swatch (no degree text), 1px/1px 50%-black shadow, swatch floats past
  the ring.
- "Inspector" adopted as the name for hexagon + hovered/pinned readouts;
  centered in the header via equal flex side regions.
- Mini hexagon in the header grew into a full widget: click toggles
  81/162 (floating only, dock locked small), drag detaches to a
  floating card (window-listener drag survives the dock unmounting),
  iPadOS flipped-L corner grip resizes 56-360px, drop-on-header or
  clicking the persistent square "return color hex" slot re-docks.
  Slot is aspect-square + self-stretch so the header never shifts;
  header-wide group-hover hints interactivity.
- Twilight legend ring: hovering the hue tile paints the hexagon's
  circle as a conic gradient replicating the shader ramp (11px thick),
  anchored to the live target hue.

## Controls & mobile

- Control scheme: RMB pans (context menu suppressed), LMB drags the
  selection loop from anywhere (4px arming), two-finger pan/pinch on
  touch, one finger samples, manual double-tap framing (touch-action
  none suppresses synthesized dblclick).
- Header snaps to compact layout below xl/1280 (subtitle out below
  1440); compact = logo + icon buttons row, centered Inspector row,
  floating hexagon. Bug fixed: float-on-compact ran only at mount —
  now a matchMedia listener (auto-floats undo on widening; user floats
  stay).

## Readouts, demos, color

- Readout rows: value bars under every number (channel inks), bold
  group titles, int/float RGB setting, blank-but-present empty rows
  (outlined swatch + dimmed #), app reticle icon for the hover row.
- Three built-in demos (SMPTE + Taylor's Linear/Radial Rainbow) as
  library rows with PgUp/PgDn order and full details.
- Color space sniffing in library details; Color math Linear/sRGB
  setting (tiles vs readouts consistency); HSB↔HSL cross-fades.
- Twilight is the default hue map; other styles behind the Labs flask.
- Tint fade fixed for real: sRGB-space mix at constant rate (linear-mix
  gamma made fade-to-white read as a snap).

All on dev; main still at v0.1 pending next promotion.
