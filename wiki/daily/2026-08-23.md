# 2026-08-23

## Project kickoff — Channel Surfer

- Reviewed the Gigi prototype (ImageViewerGigi) and the abandoned Godot
  Image-Breakdown attempt (dissolve demo only, never reached the theme).
- Decisions locked with Taylor: fresh repo, slug **channel-surfer** /
  display title **Channel Surfer** (Taylor liked the name), Next.js+R3F+
  shadcn stack, GitHub Pages, 3-tier branching, image-upload MVP, images
  local-only (IndexedDB).
  → created [[decision-web-refactor-stack]]
- Taylor shared future interactivity wishlist (displacement maps,
  click-to-zoom, tile hot-swap, hover hue recalibration).
  → created [[backlog-interactivity-ideas]]
- Scaffolded: create-next-app (bun), shadcn init + button/slider/select/
  separator/label/tooltip, three/R3F/zustand, export-gated next.config,
  Pages workflow (wright-angles pattern), this wiki.
- Ported MakeTiles.hlsl → GLSL fragment shader (feature/breakdown-mvp).
  Verified on-GPU via headless pixel readback (browser pane was hidden, so
  no screenshot — R3F won't mount in a hidden tab; see global memory).
- Taylor asked for Figma/Miro-style canvas navigation → refactored the
  quad onto a world-space plane under an orthographic camera with drei
  MapControls (drag pan, wheel zoom-to-cursor, nearest-filter pixel
  peeping). Camera-based on purpose: displacement-map future wants it.
- created [[test-plan-2026-08-23-breakdown-mvp]] — visual checks for
  Taylor since the pane was hidden during dev.

## Tile interactivity (feature/tile-interactivity)

- Implemented three items from [[backlog-interactivity-ideas]]: hover
  outlines, hover-driven hue recalibration (uTargetHue uniform, CPU pixel
  sampling from a cached ImageData, circular-shortest-path tween back to
  180° on leave), and double-click tile framing (camera tween; user grab
  cancels in-flight tweens).
- Replaced the static label grid below the canvas with a floating
  hover label above the tiles (Taylor's call), plus a gap-center
  crosshair cursor over the hue-map tile.
- GPU-verified: target hue = hovered pixel's hue renders that pixel
  white (the white-point recalibration behavior).

## RGB colorize toggle (feature/rgb-colorize-toggle)

- Single click on any R/G/B tile toggles the trio between black→white and
  black→channel-color (uRgbColorize uniform, cross-faded in useFrame).
  Click is guarded: drag-release ignored via e.delta, and the toggle waits
  250 ms so a double-click (framing) cancels it.
- GPU-verified both modes plus the halfway blend.

## Evening batch (three branches)

- **feature/visual-restyle** — adopted color-taylor's visual language:
  Barlow + Share Tech Mono (fontsource), the anodised oklch token ramp
  (hairline alpha borders, surface-inset, shadow elevation), and bumped
  every shadcn component to text-base per the UI type rule.
- **feature/hover-polish** — Taylor reported white flashes on RGB tiles
  when crossing between them: drei's Line2 outline renders one frame with
  a stale resolution uniform on every remount. Outline moved into the
  fragment shader (uHoverTile + constant-screen-width edge). Hue
  recalibration now has a persisted 3-state setting (off / hue tile /
  all tiles, default all) in a color-taylor-style right sheet; reticle
  cursor follows wherever picking is active.
- **feature/media-library** — IDB becomes a real library (multi-record,
  legacy single-image migrated), right panel with thumbnails, select /
  remove, multi-file drop, and header-parsed details (format, color mode,
  bit depth, progressive flag, file size) via magic-byte sniffing in
  [[../notes/decision-web-refactor-stack|the stack's]] lib/image-details.ts.
- **feature/tile-keyboard-nav** — arrow keys step between neighboring
  tiles while framed (clamped at edges, keys swallowed so the page never
  scrolls, form controls left alone).
- **fix/ux-defaults-reticle** — library closed by default, highlight
  default now "Hue tile" (persist v1 migration), reticle regrown to 32px
  with a 14px clear center window.
- **fix/snap-interactions** — REAL flicker root cause found after Taylor
  reported it persisted: the inline `uniforms={{...}}` prop is a new
  object each render, so hover re-renders made R3F re-apply it and reset
  every uniform (uRgbColorize → 0, cross-fade restarts = grayscale
  flicker on the RGB tiles). Fixed by memoizing the uniforms object per
  texture. Also per Taylor: hue retarget and arrow-key navigation now
  snap instead of tweening (double-click framing keeps its tween).

## Night batch — the big 12-item list (+ mid-flight additions)

Decisions via Q&A: toggle named **Tint**; HSL as an HSB↔HSL model toggle
(not extra tiles); **one** pin; extras approved: export PNG, Esc, copy
hex, dbl-click-outside recenter. Title: "Channel Surfer 🏄🌈".

- **feature/color-model-readouts** — HSB↔HSL setting (rgb2hsl in shader,
  tiles 4/5 + readouts + steps switch), header hover/pin color chips
  (click = copy hex), color-taylor EquationsPanel port behind a "Color
  steps" setting, hue-map style setting (warm/cool | glow | bands) as
  three live answers to Taylor's "how should the hue tile read" question.
- **feature/pin-and-focus** — click pins a sample (feathered ring on all
  tiles; cursor echo ring at 50% alpha on the other eight), Tint bar
  floats below hovered RGB tiles (replaces click-toggle), crosshair
  always on, frame margin shows neighbors, arrows wrap left/right / clamp
  up/down, Esc unframes or unpins, Space peeks source, dbl-click outside
  recenters, mask-icon isolate toggle in the tile title widget (discard
  in shader), header PNG export. Scene↔DOM via non-reactive canvasBridge
  (react-hooks/immutability forbids prop-carried mutable refs).
- **feature/library-nav** — add auto-opens the panel, "Media Library"
  naming, PgUp/PgDn image stepping.
- Verified by compiling the exact shipped fragment shader on-GPU via a
  temporary public/ probe file: isolate alpha, HSL, tint, peek all check.

## Promoted to main 🎉

Taylor gave the go-ahead after the hexagon hover card landed (locked
per-tile placement outside the hovered tile, tweening between tiles).
`dev` merged to `main` as "Channel Surfer v0.1" — the full app is live
at https://redlamp.github.io/channel-surfer/ (dev slot continues at
/dev). Twilight hue map default, Labs for the experiments, sRGB
color-space sniffing in the library, Linear/sRGB math toggle.

## Research

- created [[../research/hue-direction-encoding|hue-direction-encoding]] —
  how other fields encode CW vs CCW hue rotation (cyclic colormaps, phase
  portraits, vectorscopes, grading UIs), with shader-ready candidates for
  the hue-map tile.
